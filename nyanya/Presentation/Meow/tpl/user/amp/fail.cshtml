<%
    var i = 0, len = Items.length, o = null;
    if(len>0){
        for(;i<len;i++){
        o = Items[i];
%>
<li>
    <div class="jqm-index-funds">
        <a href="/amp/order?id=<%=o.OrderIdentifier%>">
            <div class="funds-block no-arrow">
                <label>订单金额</label>
                <strong><%=o.Principal%><samp>元</samp></strong>
                <span>预期收益:<em><%=zim.autoFixed(o.Interest)%><samp>元</samp></em></span>
                <ins><%=o.OrderTime.split('T').join(' ')%></ins>
                <del>支付失败</del>
            </div>
        </a>
        <div class="income-block">订单号: <%=o.OrderIdentifier%></div>
    </div>
</li>
        <% }
    } else { %>
<li class="no-orders"><p>亲，暂时没有失败订单哦！</p></li>
    <% } %>